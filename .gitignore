# ── 依赖 ────────────────────────────────────────────
node_modules/

# ── VitePress 构建产物 ────────────────────────────────
docs/.vitepress/dist/
docs/.vitepress/cache/

# ── 环境变量 ─────────────────────────────────────────
.env
.env.local

# ── Kiro IDE 工作区文件 ───────────────────────────────
# Spec 流程产出（需求/设计/任务文档），属于开发元数据
.kiro/

# ── 归档文件夹 ────────────────────────────────────────
# 项目指导文档，仅供本地参考，不参与部署
_archive/

# ── 测试 & 脚本（本地工具，不部署） ─────────────────
code/
scripts/

# ── 系统 / 编辑器 ────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
